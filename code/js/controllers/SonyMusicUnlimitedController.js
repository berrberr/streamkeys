;(function() {
  "use strict";

  require("BaseController").init({
    playPause: "#PlayerPlayPause",
    playNext: "#PlayerNext",
    playPrev: "#PlayerPrevious",
    mute: "#PlayerToggleMute"
  });
})();
