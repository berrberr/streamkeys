;(function() {
  "use strict";
  var BaseController = require("BaseController");
  new BaseController({
    siteName: "Studio Link Standalone",
    playPause: "#buttonmute",
    playState: "#buttonmute.btn-primary"
  });
})();
